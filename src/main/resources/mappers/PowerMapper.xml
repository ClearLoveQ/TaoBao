<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper  
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">  
  
<mapper namespace="cn.tedu.store.mapper.PowerMapper">
	<!-- 查询此人的权限 -->
	<select id="backPower" resultType="Integer">
		select power from t_power
		where id=#{id};
	</select>
</mapper>